//Задание 1. Создание таблицы и изменение данных
//Задание: Создайте таблицу EmployeeDetails для хранения информации о сотрудниках. Таблица должна содержать следующие столбцы.

Create table homework_EmployeeDetails (
EmployeeID Int Primary Key,
EmployeeName varchar(20),
Position varchar(20),
HireDate Date,
Salary numeric
)
insert into homework_EmployeeDetails (EmployeeID, EmployeeName, Position, HireDate, Salary) values (1, 'John Smith', 'Sales Manager', '2022-01-15', 55000); 
insert into homework_EmployeeDetails (EmployeeID, EmployeeName, Position, HireDate, Salary) values (2, 'Jane Doe', 'Marketing Specialist', '2022-06-30', 48000);
insert into homework_EmployeeDetails (EmployeeID, EmployeeName, Position, HireDate, Salary) values (3, 'Emily Johnson', 'Software Rngineer', '2023-03-22', 65000);

// Задание 2: Создание представления
// Задание: Создайте представление HIghValueOrders для отображения всех заказов, сумма которых превышает 10000. В представлении должны быть следующие столбцы
OrderID(идентификатор заказа),
OrderDate(дата заказа),
TotalAmount(общая сумма заказа, вычесленная как сумма всех Quantity*Price) 

create view HIghValueOrders as 
select 
o.OrderID,
o.OrderDate,
sum(od.Quantity*p.Price) as TotalAmount
from  homework_orders o
join homeWork_orderdetails od on o.OrderID = od.OrderID
join homework_products p on od.ProductID = p.ProductID
group by o.OrderID, o.OrderDate
having sum(od.Quantity*p.Price) > 10000;

//Задание 3: Удаление данных и таблиц
//Задание: Удалите все записи из таблицы homework_EmployeeDetails, где Salary меньше 50000. Затем удалите таблицу  homework_EmployeeDetails из базы данных.

-- Удаление данных--
delete from homework_EmployeeDetails where Salary < 50000;

-- Удаление данных--
drop table  homework_EmployeeDetails;

//Задание 4: Создание хранимой процедуры
//Задание:  Создайте хранимую процедуру GetProductSales с одним параметром, ProductID. Эта процедура должна возвращать список всех заказов, в которых участвует продукт с заданным ProductID, включая следующие столбцы:
OrderID(идентификатор заказа)
OrderDate(дата заказа)
CustomerID(идентификатор клиента)

Create procedure GetProductSales(in p_ProductID Integer)
BEGIN
	SELECT 
	o.OrderID,
	o.OrderDate,
	o.CustomerID
	FROM homework_orders o
	JOIN homework_orderdetails od on o.OrderID = od.OrderID
	WHERE od.ProductID = p.ProductID;
end;
	
